DECLARE @CombinedResults NVARCHAR(MAX);
SET @CombinedResults = '';

select @CombinedResults = @CombinedResults + id + "," + username + "," + bankShortname
from hj_agent where username = 'dAdam520';

-- 更新 Config 表
UPDATE Config
SET value = @CombinedResults
WHERE code = 'IUZON';